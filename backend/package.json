{
  "name": "untitled-faith-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "deploy:check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "session:dev": "node --env-file=.dev.vars scripts/create-dev-session.mjs"
  },
  "dependencies": {
    "jose": "^6.2.12",
    "stripe": "22.6.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.6",
    "@types/node": "^26.0.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.130.0"
  },
  "overrides": {
    "sharp": "^0.35.4"
  }
}
